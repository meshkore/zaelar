"""nucleo/flash/ — FlashBrain: the layer reflective sub-second of the cerebro v2.

Codigo own (no a agent externo) that closes each turn of voice. Router of input + cliente of model
fast no-razonador (model POR INVOCACIÓN) + gestor of frontend/widgets + launcher of procesos + escalado
al SlowBrain. Se enchufa al motor of voice como provider `livekit.agents.llm.LLM` (V2-004). Esqueleto in V2-001.
"""
