- User asked: al hacer clic en un cluster (en la lista), se debe abrir DESDE EL LATERAL de la propia tarjeta una vista MÁS CORTA/resumida de ese cluster (estado conexión, peers, últimos mensajes) — no navegar a otra vista ni sustituir la lista. Implementado como panel deslizante a la derecha (`.rcr-panel.rcr-open`, width 0→280px) que convive con la lista (`.rcr-list`), togglable por clic (clic de nuevo en el mismo cluster lo cierra). No confundir con `cluster-registro` (ese es el registro/chat completo con envío de mensajes); este widget es solo LECTURA y muestra una cola corta de turnos por cluster (`MAX_TURNS_PER_CLUSTER=6`), pensado para un vistazo rápido de varios clusters a la vez.
- Read-only (no `apply_action`) → foreground-only a propósito, sin `background`/`tick`: refleja los mismos logs que `cluster-registro` bajo demanda, no hace falta volcar nada nuevo a memoria.
