# cluster-registro — notes

- 2026-07-01 · Purpose: **registro íntegro** del cluster MeshKore `arena`. Backend lee kind=='cluster' de `.meshkore/logs/timeline-latest.jsonl` + `.meshkore/logs/sessions/*.jsonl`; dedupe por (t_ms, dir, texto[:80]).
- Cada turno muestra: **quién** (zaelar/peer con nombre), **dirección** (⇢ out / ⇠ in / note), **timestamp** local y **texto completo**.
- **Lista scrollable, más reciente abajo** (auto-scroll al bottom en render). No reordenar cronológicamente al revés.
- Filtrar filas puramente de estado (connected/disconnected/error/joined sin texto). Mantener las "note" internas de zaelar que sí llevan texto.
- Texto de peers **siempre por `textContent`** (XSS). CSS `white-space:pre-wrap` para respetar saltos de línea del peer.
- Cap: últimos 400 turnos.
- 2026-07-23 · `data.py` consulta el estado REAL de conexión en `GET /api/meshkore/status` (loopback, stdlib `urllib`, timeout 2s, `PORT` env con fallback 43917) — `connected`/`live_reachable`/`online_peers` en el payload. Header muestra un punto+etiqueta "conectado"/"desconectado" (verde/rojo) SOLO si el endpoint respondió (`live_reachable`); si no es alcanzable, no se pinta nada (no inventar estado). No tocar la fuente de logs existente ni el resto del layout.
- 2026-07-23 · Título del widget renombrado a **"Mesh for Cluster"** (petición del operador) — `manifest.json:title` y el `<b>` de cabecera en `widget.js` (antes "Cluster arena"/"Cluster {data.cluster}"). El **ID real del cluster** (`data.cluster`, p.ej. "arena") no se pierde: pasó al subheader `sub` como `ID <cluster> · N turnos · peers`. Este widget ES la interfaz donde se gestiona/visualiza el cluster (no hay otro panel fuera de `widgets/cluster-registro/` que tocar) — el cambio queda reflejado aquí para que se vea actualizado. No tocar el resto del layout/estilos/lógica de logs.
- 2026-07-23 · **Chat wall integrado** (petición del operador: "el chat tenga un chat wall integrado") — añadida una caja de texto + botón enviar (`.crgbar`/`.crgta`/`.crgsend`) debajo de la lista, Enter para enviar (Shift+Enter = salto de línea). `data.py` gana `apply_action("send", {text})` → `POST /api/meshkore/send` (loopback, mismo host/port/timeout que `_live_status`; pasa por `scan_outbound` en el server). Declarada en `manifest.json §actions` con `"confirm": true` (enviar es irreversible) — única data-op del widget. Error de envío se muestra inline (`.crgerr`) y conserva el texto tecleado para reintentar. No se ha tocado la lectura del registro/logs existente ni el resto del layout.
- 2026-07-23 · **`apply_action("send")` refleja el cambio REAL de inmediato**: tras un envío `ok`, si el turno recién enviado aún no aparece en el `view_data()` re-leído (el log del cluster se escribe de forma async, puede ir por detrás del POST), se añade OPTIMISTA al final de `turns` (dir="out", who="zaelar", ts=ahora) para que el estado devuelto no mienta ("ok" sin que se vea el mensaje). Se deduplica contra los últimos 5 turnos por (dir="out", texto exacto) — si el log ya lo trae, no se duplica. No toca la ejecución real (`_send_message`/`POST /api/meshkore/send`) ni el resto de `view_data`/layout.
