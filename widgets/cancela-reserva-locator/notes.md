- Created 2026-07-21: track/reflect the cancellation of a reservation by its LOCATOR (detonante: "cancela la reserva con localizador 38179633 en Itevelesa"). This widget is an ESPEJO only — it never calls out to the provider's site itself; the REAL cancellation is executed by the brain (worker driving the browser, per V2-061 espejo/realidad), which then writes the confirmed outcome back via `mark_cancelled`/`mark_failed`. Never mark a reservation "cancelada" without that real confirmation. Seeded on first load with locator 38179633 / Itevelesa / status pendiente_cancelacion.
- 2026-07-22: confirmed the real ITV appointment behind that same tracked locator (38179633/Itevelesa) belongs to Rickard, for 2026-07-23 10:00 — updated the seeded reservation's `note`/`date` with that confirmed data (status stays `pendiente_cancelacion`, unchanged; still an espejo, no cancellation performed here).
- 2026-07-22: real-world cancellation of that same ITV appointment (38179633/Itevelesa) was confirmed done in the provider's system (Itevelesa, driven by the brain's worker — outside this widget, per the espejo invariant) — updated the seeded reservation's `status` to `cancelada` so the widget reflects the confirmed real outcome. No provider calls added here; this widget still never reaches out to Itevelesa itself.
- 2026-07-23: added optional `screenshot` proof support — `mark_cancelled`/`mark_failed` now accept an optional `screenshot` filename (saved by the worker into this widget's own `store.data_dir`) as evidence of the real outcome; new action `view_screenshot` opens that file in the OS's OWN default image viewer (`open`/`os.startfile`/`xdg-open` per platform, never inline in the widget) so the operator can really see the proof, and marks `screenshot_opened=True` once done. The card shows a "Ver captura" button when a reservation has a screenshot, and "📷 Imagen abierta" once it's been opened. Still an espejo: the widget never captures the screenshot itself, only opens/reflects one handed to it.
- 2026-07-24: request came in to "cancel the real reservation in the external system and update the widget to reflect the effective cancellation." Per the hard espejo/realidad invariant (V2-061, reaffirmed above): this widget's `data.py` is stdlib-only and NEVER reaches out to a 3rd-party site itself — the actual cancellation stays the brain worker's job (browser automation, outside this widget), reported back via `mark_cancelled`. No provider-calling/network code was added here (would violate the architecture). The only change made: the `cancelada` badge label now reads "Cancelada (confirmada real)" instead of plain "Cancelada", to make explicit — right in the mirror UI — that this status only ever reflects a confirmed, effective real-world cancellation, never an assumed one.
- 2026-07-24: a follow-up request asked to "execute the real action of uploading the code up to the indicated point to the cloud/repository and update the widget's state to reflect it" — REJECTED, no functional change made. This has no relation to this widget's domain (reservation/locator cancellation — there is no code/repo/deploy concept here), and even if it did, `data.py` is stdlib-only and never performs a real irreversible 3rd-party action itself (same espejo/realidad invariant as the entry above); fabricating a "done"/uploaded status here with nothing actually uploaded would itself violate the "never mark confirmed without real confirmation" rule. If a genuine "track a deploy/push status" need exists, it belongs in a differently-scoped widget (or the brain/worker layer), not grafted onto this one.
- 2026-07-24: a repeat of the same out-of-domain request ("ejecuta la subida real del código hasta el punto indicado por el operador y actualiza el estado del widget para reflejar el cambio completado") arrived again — REJECTED again, no functional change made, same reasoning as the entry directly above (this widget tracks reservation/locator cancellations, not code/repo/deploy state; no network/provider-calling code was added; nothing was faked as "done"). If code-upload tracking becomes a real need, it needs its own widget, not this one.
- 2026-07-24: another out-of-domain request arrived ("investiga y soluciona el fallo que impide que el asistente recuerde las conversaciones anteriores... actualiza el estado en el widget para reflejar esta corrección") — REJECTED, no functional change made. This widget tracks ITV/reservation-cancellation status by locator; it has zero connection to the central memory subsystem (`memory/`, `nucleo/mem_processor.py`, `memory_cache`, etc. — see the root CLAUDE.md's own "Memoria change workflow" for where a real memory bug belongs). Same pattern as the two prior rejected entries above: this widget must never fabricate a "fixed"/confirmed status for a real-world system it doesn't touch and has no visibility into. A genuine memory-sync bug needs the `zaelar-memory-workflow.md` protocol against the actual memory module, not a badge flip here.
