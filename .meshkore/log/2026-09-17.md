# 2026-09-17

## 13:45 · V2-715 · el directorio de contactos se ordena solo y una plataforma es un filtro

Suyo, con la tarjeta abierta sobre sus 2 688 contactos: «de estas tres líneas iniciales del widget —la barra
negra, la barra de búsqueda y la barra de selección de All People Places— todo eso hay que convertirlo en dos
barras», «en la barra lateral es donde vayamos a desarrollar de forma dinámica todo lo que tenemos»,
«cuando entramos en Telegram quiero ver solo los contactos de Telegram» y «varios teléfonos vinculados a la
misma empresa».

Entregado: UNA barra (la ventana ya es la otra, así que el disco de marca y el título repetido salen); el
raíl se talla de lo que el directorio tiene (tipos, etiquetas, ciudades, ocultos) y no dibuja lo que no
existe; el icono de una fuente enlazada FILTRA y el de una sin enlazar sigue abriendo Conectores; `phones` /
`emails` con etiqueta, con el escalar como PRIMARIO en paso con la lista (cuatro módulos fuera del widget lo
leen); esquema v1→v2 migrado fila a fila dentro de su propio `try`; tope de 120 filas con «ver más»; y las
acciones que faltaban para que cada botón tenga voz (`add_phone`, `add_email`, `show_connectors`,
`show_view {hidden}`).

Tres defectos medidos que no se veían: `show_view` mandaba `kind` y `source` desde V2-714 y la tarjeta no
leía ninguno; Google solo importaba el teléfono principal; `link_contact` existía sin control desde V2-541.
Y tres cosas ya rojas en árbol limpio, arregladas: dos tests de contactos rancios y el nodo 4.188 duplicado.
Nodo 4.190, 18 desarmes todos rojos. Commits `d83602a0`, `a046c03b`. Motor reiniciado y verificado en vivo
contra su directorio real (Telegram 1825, Google 2687).

## 15:45 · Estabilización del repo · el daemon deshizo la partición de CLAUDE.md, y nadie lo vio

El operador: «toma el control y estabiliza el repo». El árbol llevaba tres ficheros modificados y cuatro sin
seguir desde el 2026-09-16 a las 14:09, y dos sesiones seguidas los habían marcado como «no míos» sin tocarlos.

**La causa.** `bba7aac8` (2026-09-15) partió `CLAUDE.md` —el diario a `docs/decisions.md`, el inventario a
`docs/modules/zaelar-module-map.md`— y pasó cinco copias por CLI a dos. Editó los RENDERS y no la FUENTE:
`.meshkore/public/AGENT_INSTRUCTIONS.md` seguía con el bloque OPERATOR_CONTENT viejo de 400 KB. La siguiente
pasada del daemon regeneró los cinco desde ahí y deshizo la partición entera en un instante, en silencio:
CLAUDE.md 57 KB → 402 KB, AGENTS.md de puntero a copia, y `GEMINI.md`, `.clinerules` y `.cursor/rules/`
resucitados. Su propio trinquete llevaba dos días rojo.

**Lo comprobado antes de reescribir nada**, porque el riesgo era perder texto: de las 3 874 líneas del bloque
viejo, todas tienen casa en el nuevo ∪ `decisions.md` ∪ `decisions-archive.md` ∪ `zaelar-module-map.md` salvo
103, que son prosa REESCRITA más compacta — verificado concepto a concepto (run_testmap, make run,
BRAIN=nucleo, LiveKit, INI-013, Colmena…), no por comparación de líneas. Copia de seguridad de los seis
ficheros fuera del repo antes del primer `write`.

**El arreglo va en la fuente**: OPERATOR_CONTENT de `AGENT_INSTRUCTIONS.md` ← el bloque partido, preámbulo del
daemon intacto (el suyo era MÁS NUEVO: trae §28 v34, delegación). CLAUDE.md se re-renderiza desde ahí. El
daemon volvió a escribir AGENTS.md **mientras yo trabajaba** — ya con el contenido bueno, 57 KB — así que se
acepta como render en vez de pelearla: con la fuente correcta ninguna copia puede ir por detrás, que era todo
el objetivo de la partición. Las otras tres se borran y se gitignoran con el porqué escrito.

**De paso, los dos trinquetes que llevaban días rojos y nadie había mirado**: `tester.md` apuntaba a
`.meshkore/workflows/INDEX.md`, que no ha existido nunca en este repo (ahora apunta a `tests/README.md` y al
playbook, y el perfil dice que aquí no hay W10); y el diario citaba V2-700, V2-701 y V2-703 sin fichero de
iniciativa — reconstruidas desde su entrada del diario y marcadas como tales, sin inventar el plan previo.

`tests/infrastructure/` entero: **1085 verdes, 0 rojos**.

## 18:15 · V2-717 · una sola caja, y la canción ocupa la pantalla

Suyo, con la tarjeta de música abierta y una canción sonando: «has puesto un contenedor exterior con el
título y los botones del sistema, y dentro otra caja como si eso fuera el widget — yo solo quiero UNA caja
encima del escritorio», «me gustaría ver la canción en toda la pantalla del widget: la imagen más grande, el
nombre, el artista, la duración, una barra de progreso para que yo la pueda mover, y sin nada más», «está un
poco triste al verse tan vacía».

Entregado: `.hb-mus2` pierde borde, radio y fondo — el canvas ya dibuja la ventana, así que la tarjeta de
dentro era un marco encima del marco; UNA barra (el título repetido fuera), con un conmutador DERIVADO que
solo existe mientras suena algo y la pastilla de fuente como puerta a la pantalla de fuentes; la cara AHORA
SUENA con portada grande, ficha y carril de tiempo arrastrable, con el transporte abajo como él pidió; la
biblioteca vacía con algo que decir y tres pastillas que reproducen de verdad.

El cabezal de reproducción **no se guarda en el store y no debe**: el fichero local y el iframe de YouTube
suenan en su página y tienen su reloj (arrastrar los mueve sin servidor); Spotify suena en otro aparato, así
que su `progress_ms` es una fotografía que la tarjeta adelanta y arrastrar es ida y vuelta. Por voz,
`seek {to|by}` deja una orden numerada que aplica la página — con contador propio y nunca `cmd_seq` (si no,
la canción salta atrás al tocar el volumen), descartada al cargar otra canción, y sin tocar `local.seq`.

El reloj del reproductor gratis ya estaba ahí: el handshake `listening` de V2-047 entrega `infoDelivery` con
`currentTime`/`duration`, cero llamadas nuevas — filtrado por id, que es lo que impide que el reloj del
widget de youtube se convierta en la posición de esta tarjeta.

Nodo 4.3 ampliado (18 casos nuevos) + 7 en `test_data.py` + 4 en el provider de Spotify. **26 desarmes, los
26 rojos** — dos costaron tres intentos porque el test pasaba desarmado: un carril que pide `width:100%` de
un hueco sin anchura mide 0 píxeles, y una columna flex que centra su contenido lo RECORTA al desbordar sin
que `scrollHeight` se entere (el caso mide contención a cuatro tamaños de tarjeta). `make test-widgets`
14/14, y ahí saltó un fallo real: la clase `.drag` colisionaba con una regla global del shell.

⚠️ **NO reiniciado**: hay un encargo VIVO esperando respuesta (quedan 3 h) y sesión abierta hace un minuto.
El widget nuevo necesita reinicio para que entren `data.py` y los conectores — queda pedido, no hecho.
