
## 07:35 · Empty canvas states itself in the prompt (#T-empty-screen-state)
Session 3955f666: operator closed all widgets (canvas reported `instances: []`), asked for a video list, and FlashBrain claimed the YouTube card was already open. Root cause in the trace, not the frontend: `memory.compose_state` listed open widgets only when non-empty, so an empty screen composed silence and the model filled it from `recent_widgets` (youtube) + rails. Fix in `memory/_prompt.py`: an explicit `Pantalla VACÍA` fact whenever the block is non-blank — a declared state, not a behavior sentence. Blank-state and e2e empty-canvas pins hold (commit `d9e03424`, 18 tests green, disarm-proven).

## 14:47:35 · snapshot 20260919-144735872-rjj-mac-claude-mic-capture-glow-operator-green-342c
- agent: `rjj-mac-claude` · conv `general-09181543`
- note: mic capture glow (operator green) + scale
  - `frontend/app/styles.css` (127274 B)
  - `frontend/mobile/app/styles.css` (28687 B)
- restore: `GET /snapshots/20260919-144735872-rjj-mac-claude-mic-capture-glow-operator-green-342c/files/<path>`

## 14:48:43 · snapshot 20260919-144843621-rjj-mac-claude-register-mic-glow-test-in-node-4-3361
- agent: `rjj-mac-claude` · conv `general-09181543`
- note: register mic glow test in node 4.152
  - `tests/run_testmap.py` (470057 B)
- restore: `GET /snapshots/20260919-144843621-rjj-mac-claude-register-mic-glow-test-in-node-4-3361/files/<path>`

## 17:30 · widget open fixed: adb76984 broke every new card (TypeError before _place)
- Anchor #T-widget-open-regression (initiative `voice-session-fixes`): backend catalog/data/JS all 200, so the dead open was frontend-only — show()'s fresh branch read `w.card` while `w` is undefined, plus undeclared `background` in _renderAliases/createWidget-error/maximize. Reverted those 4 hunks (commit 21496dc8), pinned with node 4.149 source test + disarm (commit db498677), pushed. Pre-existing unrelated red left open: `test_a_sweep_still_asks...[vacía la agenda]` returns 'fast' not 'confirm' (V2-712 consent area, untouched).

## 23:30 · Jev request-type classifier drives the filler cover (#T-jev-filler, voice-session-fixes)
- Operator: filler words were incoherent ("Good question" for an order) because the pool came from regex shapes alone. New single integration point `nucleo/jev.py` asks Jev (POST api.typesafe.ai/v1/systemone, Choice over 6 request types, key TYPESAFE_API_KEY env else jev.md) on its own thread at arm time; the fire site re-picks the pool only on a confident verdict, else the regex stands. Every call emits one `jev request-type` timeline event with verdict + ms. Live probe 3/3 at conf 1.0 (~800 ms, inside the 1.1 s deadline). Tests: node 3.58, 12/12 green, disarm-proven; neighbors 49 green. Commit 494ccb1c pushed; engine restarted quiet (sessions []) — build 3.27+494ccb1c == HEAD.

## 23:48:19 · snapshot 20260919-234819680-rjj-mac-claude-add-operator-co-authored-by-over-e77a
- agent: `rjj-mac-claude` · conv `general-09181543`
- note: add operator Co-Authored-By override
  - `.meshkore/public/AGENT_INSTRUCTIONS.md` (57530 B)
  - `AGENTS.md` (57721 B)
- restore: `GET /snapshots/20260919-234819680-rjj-mac-claude-add-operator-co-authored-by-over-e77a/files/<path>`

## 23:52 · Co-Authored-By restored as operator override (commit 252d82bf)
Operator assumed co-authoring from the start; preamble §9.1 bans it. Fixed without touching the managed preamble: one bullet in OPERATOR_CONTENT (source + rendered AGENTS.md) declaring OPERATOR_CONTENT wins, every commit = MeshKore trailers + Co-Authored-By naming the authoring agent. Snapshot e77a, pathspec commit, pushed main 494ccb1c..252d82bf.
