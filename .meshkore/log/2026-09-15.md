# 2026-09-15

- Limpieza del roadmap (lote 1, #roadmap-hygiene): 42 iniciativas `status: delivered` pasadas a `done` (37 formato simple + 5 con commit: V2-640 d96f670, V2-641 6edb324, V2-642 f95327a, V2-646, V2-647) y V2-069 cerrada tras verificar que el swap está hecho (sin reasoner.py, sin brains/). Quedan ~96 con estado fantasma (open/parcial/design/plan) para revisar con criterio en el lote 2; no se archivó nada a log/ aún.

## 12:20 · V2-699 · la caja de sincronización vive DENTRO de la tarjeta de Google

Suya: «la cajita de sincronización va dentro del google connector box. no suelta. cuida el diseño y la
CLARIDAD». La tarjeta ES la fuente: `.ctsrcbox` envuelve la fila de cuenta y, para la conectada, su panel
de sincronización como mitad inferior — un borde, una línea fina, fondo más suave. Además el aviso ámbar
repetía lo que la línea de dirección ya decía; ahora solo lleva lo que la desbloquea. Regla escrita en
`zaelar-widget-header-standard.md` con su trinquete. Commit `5ee700d3`.

## 13:10 · V2-701 · la sincronización de contactos se queda ENCENDIDA y es un espejo

Suya: «no es algo que deberíamos hacer de forma puntual […] debería quedarse conectado de forma
permanente», y después: «se modifica en un sitio o en otro […] es un espejo […] si se modifica en el
teléfono, todo el sistema se sincronizará».

MEDIDO antes de tocar: la prueba de «esto es nuestro» era una FECHA (`updated`, YYYY-MM-DD), que dice
«editado hoy» hasta medianoche — inofensivo con un botón, un PATCH por contacto editado por minuto con
un temporizador, y ninguna de esas escrituras falla, así que no se habría visto nunca.

Entregado: interruptor (`sync.auto` + acción `set_auto`) en vez de botón; `tick` por el ciclo del
manifiesto con el periodo en `gcontacts.PERIOD`; token de sincronización de Google (`sortOrder` fuera —
Google no lo admite con sync, y los parámetros deben ser idénticos entre pasadas); relojes `touchedAt` /
`pushedAt`; borrados en los DOS sentidos (`pendingDeletes` + `_drop_deleted`) con cortacircuitos del 20 %.

VERIFICADO en vivo contra su cuenta real: pasada completa 3,4 s / 2 855 leídos / 0 duplicados; la
siguiente, incremental, 0,41 s y cero filas. 12 desarmes, todos rojos. Nodo 5.29. Commit `77d0f091`.

⚠️ ABIERTO: el tick no corre porque el agente está PARADO (`runstate.stopped()`), norma V2-092 — la
agenda tampoco tiquea. Y la mitad de escritura sigue sin probar: falta
`https://www.googleapis.com/auth/contacts` en la pantalla de consentimiento.

## 15:30 · V2-703 · una lista de resultados es una LISTA, y la plantilla la elige el worker

Suya, sobre la hoja de la olla que V2-702 acababa de arreglar: «los resultados aparecen en una línea
horizontal y cada cosa en su sitio. Son cajitas bien puestas, plantilladas […] el botón de ver en Amazon
es el que aparece en la ficha […] si un resultado solo ya ocupa toda la pantalla, ¿de qué me sirve la
lista de resultados?». Y después: «el worker […] debe elegir ese formato y colocar dentro los datos que
sean relevantes […] tanto si buscamos barcos como cafeteras, como recetas, como eventos históricos».

La lista suelta la tabla de specs y el enlace a la tienda (ambos son de la ficha) y el destacado se MARCA
en vez de inflarse — `primary.length === 1 ? 1 : 2` dibujaba su único resultado a todo el ancho. Una sola
rejilla, tarjetas que estiran a su fila, pie anclado abajo, presupuesto de líneas por campo.

`layout` (card|tile|row|compare) pasa a ser del worker: enmienda a la regla 1 de `presentation.py`,
decidida por él. La razón de la regla sobrevive como guarda — un NOMBRE, nunca un número; una plantilla
imposible degrada; una inventada se rechaza en `data.py`.

MEDIDO, tres veces con el desarme en verde antes de acertar: el suelo de ancho seguía saliendo de un
heurístico que cuenta `facts`/`blocks`/`images`, que la lista ya no dibuja; contar el badge en el DOM no
dice si se PINTA; y la decisión de layout había que escribirla (`data-layout`) porque `makeCard` degrada
cada tarjeta por su cuenta y el dibujo sale bien corriera la guarda o no.

⚠️ Tres rondas perdidas por un comentario CSS mal cerrado dentro del `<style>` inyectado: prosa parseada
como declaraciones, `node --check` verde, el clamp declarado e inerte. Solo se vio leyendo el estilo
COMPUTADO de vuelta.

14 desarmes, todos rojos. Nodo 4.179.
