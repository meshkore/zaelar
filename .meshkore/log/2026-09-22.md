# 2026-09-22

## 09:30 · V2-749 — el oído barato, y la palabra que el modo no exigía

El 🤖 del orbe se llama «Modo Wake Word» y no cambiaba nada de lo que cuesta el micrófono. Medido en la
sesión `48394dd0` (`stt_provider=deepgram`, `attention_mode=smart`): cada frase de la habitación se
publicaba, se transcribía y se pagaba antes de que el gate la tirara, la línea provisional se pintaba y se
borraba delante de él, y a la tercera repetición contestó con `reason: unanswered_repeat` sin que sonara
ninguna palabra de activación.

Siete piezas: el grifo de audio como TERCER eje (nunca escribe el interruptor del micro — regla del operador
de 2026-09-10 en `voice/mic_input.py`); un reconocedor del propio navegador que oye el nombre gratis y que
no puede ser un modelo de keyword porque el nombre lo cambia él por voz (V2-747); lo dicho ANTES del nombre
viajando como `before` hacia el `reclaim_ambient_tail` que ya existía; el cierre de la escotilla que abría
un turno en frío sin la palabra (revierte V2-743 **solo en `smart`**, con su test reescrito para registrar
la reversión); el muro que ya no escribe lo que va a borrar; el anuncio de la ventana que abre una pregunta
del propio agente, sin el cual el grifo aparcado sería un deadlock; y la palabra dicha en voz alta, en el
tooltip y en una nota de tres segundos donde van los subtítulos.

⚠️ Aparcar sobre un oído que no está es sordera, que es peor que cualquier factura: aparcar no es un modo
sino una consecuencia de que el reconocedor haya disparado su propio `onstart`. Sin `SpeechRecognition` todo
esto es inerte.

Nodos 4.208, 4.209, 8.15 · 20 desarmes, todos rojos · la mitad navegador NO está verificada en vivo.
De paso: `tests/watchdog.py --impacted` le pasaba a pytest los `test_*.mjs` cambiados, que no puede recoger.
