# 2026-09-22

## 09:30 · V2-749 — el oído barato, y la palabra que el modo no exigía

El 🤖 del orbe se llama «Modo Wake Word» y no cambiaba nada de lo que cuesta el micrófono. Medido en la
sesión `48394dd0` (`stt_provider=deepgram`, `attention_mode=smart`): cada frase de la habitación se
publicaba, se transcribía y se pagaba antes de que el gate la tirara, la línea provisional se pintaba y se
borraba delante de él, y a la tercera repetición contestó con `reason: unanswered_repeat` sin que sonara
ninguna palabra de activación.

Siete piezas: el grifo de audio como TERCER eje (nunca escribe el interruptor del micro — regla del operador
de 2026-09-10 en `voice/mic_input.py`); un reconocedor del propio navegador que oye el nombre gratis y que
no puede ser un modelo de keyword porque el nombre lo cambia él por voz (V2-747); lo dicho ANTES del nombre
viajando como `before` hacia el `reclaim_ambient_tail` que ya existía; el cierre de la escotilla que abría
un turno en frío sin la palabra (revierte V2-743 **solo en `smart`**, con su test reescrito para registrar
la reversión); el muro que ya no escribe lo que va a borrar; el anuncio de la ventana que abre una pregunta
del propio agente, sin el cual el grifo aparcado sería un deadlock; y la palabra dicha en voz alta, en el
tooltip y en una nota de tres segundos donde van los subtítulos.

⚠️ Aparcar sobre un oído que no está es sordera, que es peor que cualquier factura: aparcar no es un modo
sino una consecuencia de que el reconocedor haya disparado su propio `onstart`. Sin `SpeechRecognition` todo
esto es inerte.

Nodos 4.208, 4.209, 8.15 · 20 desarmes, todos rojos · la mitad navegador NO está verificada en vivo.
De paso: `tests/watchdog.py --impacted` le pasaba a pytest los `test_*.mjs` cambiados, que no puede recoger.

## 11:45 · V2-749b — «la primera vez le ha costado mucho»

Lo probó en vivo. Sesión `5789bad4`: grifo aparcado en +0,32 s, saludo con pregunta en +4 s, y el spot en
**+33,55 s** con los cinco «Johnny» dentro de un único final — el detector solo se creía un resultado FINAL
y Chrome finaliza cuando dejas de hablar, así que la latencia no era un número sino «lo que él tarde en
parar». Sesión `b41925f6` (reconexión): ni un solo `grifo aparcado`, el oído no volvió a arrancar y el modo
cayó a pagarlo todo sin decirlo — por eso la segunda vez pareció instantánea.

Tres arreglos: el spot se muda al stream de interims (grifo, ventana y anillo al instante) con el final como
respaldo que se descarta si el STT de pago ya tiene el turno; el saludo se arma como locución dirigida en los
modos que aparcan, porque preguntaba a un micrófono aparcado; y el oído reporta su propio estado, con 1,5 s
de gracia para no confundir un rearranque rutinario con una caída.

17 desarmes, cinco verdes al principio y los cinco acusaban al test.


## 20:35 · V2-751 — un titular, un suplente, y una luz que pregunta

«¿Qué podemos hacer cuando falla el modelo principal? Cuando esto pasa no funciona nada.» Medido: **DeepSeek
no estaba caído** — `/models` 200, $2,77 de saldo, chat en 1,22 s en el mismo minuto en que el panel decía
«no responde». La alarma era una luz pegada: un turno falló a las 12:21:02 y solo un turno de VOZ con éxito
la apagaba, así que sin micrófono abierto se quedaba roja los 600 s del TTL.

Y la escalera era decorativa: `voice_brain` sin relevo (documentado como riesgo conocido a la espera de su
decisión) y la cadena que debía relevar apuntando al mismo host que el titular. Su máquina, además, tenía el
relevo vaciado por un gate de `is_cloud_account`.

Entregado: la tabla a un titular + un suplente en todas las filas (nunca el mismo host, OpenAI nunca
primero), la escalera de memoria de tres escalones a uno y sin broker, la cadena de voz leyendo la tabla, el
vigilante del titular (10 s → 60 s → 3 min, solo puede poner verde), el panel en ámbar con dos líneas de dos
colores, y cuatro copias de la tabla convertidas en lecturas. Titular de voz a V4.1-Flash por decisión suya.

13 desarmes, todos rojos. **No se reinició**: encargo vivo `18a847-1`.
