
## 13:10 · V2-709 · el visor de tests: idioma → familia → tema, el plan antes de la corrida

El operador pidió arrancar el visualizador y simplificar el sistema de testing. Medido antes de tocar
nada: de los seis runners bajo `tests/`, cinco emitían el protocolo de eventos y `use_cases/e2e/agent`
—el tier que más le importa, el agente inteligente negociando contra el producto— **no emitía nada**.
Con una ronda corriendo, lo único en pantalla era `process.output`. Ésas eran las mil líneas.

Decisiones suyas en la sesión: la voz pasa a ser un CANAL de un caso de uso, `journey` se absorbe como
caso de uso largo, y el selector de idioma manda sobre casos de uso y memoria pero no sobre integridad.
Las tres pendientes de ejecutar (F4/F5 de la iniciativa).

Entregado: `families.py` (tres familias, dos idiomas, el idioma como índice y no como filtro),
`themes.py` (once agrupadores con tabla explícita, 166 casos clasificados), 104 títulos ES pasados a
castellano descriptivo, `plan.py` + `e2e/agent/bus.py` (el plan que el catálogo pinta es el que el runner
marca), y el visor reescrito entero: menú plegable sin «Todos los tests», panel que cuenta ACCIONES, un
caso por línea con su plan dentro, y la canaleta de eventos como cajón cerrado.

Dos cosas pagadas: (1) la primera versión del test del plan comparaba dos lecturas de la MISMA lista y su
desarme salió verde — reescrito para leer los literales del AST de `run.py`; (2) el ▶ de «Memoria»
significaba el gateway conversacional y ese hecho vivía como literal en el JavaScript del visor, así que
la reescritura lo convirtió en silencio en «todos los deterministas de memoria». Ahora
`memory/suite.json` declara su `primary_case` como el resto.

Commits: `98cc28c9`, `2cf1a966`. Iniciativa: `.meshkore/roadmap/initiatives/V2-709-*` (gitignorada).

## 20:00 · V2-711 · el raíl vuelve a la consecuencia: plan medido, listo para ejecutar

Estudio del cerebro sobre el árbol actual, todo medido en aislado. Cuatro ejes con agujero real:

1. El gate del clic irreversible está en UNA de cuatro rutas (`click` por ref); `click_at`, `press` y
   `type --submit` pulsan sin él — y la CLI del worker recomienda justo la de visión. Decide por rótulo:
   21 de 30 etiquetas reales pasan («Reservar», «Firmar», «Send money»). Y falla ABIERTO si no puede
   describir el elemento.
2. Tres mecanismos entregados sin llamante vivo: `rows.*` (V2-707 F1) está en `deny` para el worker que la
   pidió, y las sombras del árbitro y de los encargos no tienen lector, así que su puerta de promoción no se
   puede evaluar.
3. Cero tests ejecutan `NucleoLLMStream`; lo que corre el arnés es `probe.py`, y los dos canales se cosen a
   mano con 29 marcas «cablear en AMBOS».
4. `contract.guard` falla abierto y `server_api` lo envuelve otra vez; `_note_done` pregunta la FRICCIÓN para
   saber si algo se borró, así que `cancel_meeting` entra en el libro como no destructivo.

Dos correcciones de la segunda pasada, que cambian el trabajo: el trinquete de espejos YA EXISTE y cuenta
«impl PARALELA» (13, techo 15, verde) mientras la marca que se usa de verdad va por 29 e es invisible — se
extiende el que hay, no se añade uno; y el contador de descartes ya está en `bus/log.stats()`, lo que falta
es exponerlo y contar los otros dos descartes mudos. Restaurar `danger.py` es injerto a mano, no checkout:
`87874bd7` cambió también el código.

Decidido NO hacer: ampliar la tabla de verbos de `danger.py` (más verbos = más workers aparcados),
`cancel_meeting` a CONFIRM (contradice V2-707 F0/F1), la cruzada de manejadores mudos, tocar `party.py`.

Plan en `roadmap/initiatives/V2-711-el-rail-vuelve-a-la-consecuencia.md`: T0 siete cortes con desarme (una
jornada), T1 la puerta única del clic por contexto + lector de sombras, T2 el test del turno real + techo del
prompt compuesto + F4 acotado, T3 retirar el aparcamiento por texto con puerta y `decide_turn` por rebanadas.
A la espera del OK del operador para implementar.

## 19:40 · V2-711 ejecutada — T0 completo, T1 completo, T2 parcial (4 commits)

`e50c1cc9` T0.1–T0.4 · `c95ed990` T0.5–T0.7 · `222d8bd4` T1 · `d853f988` T2 parcial.

**Entregado.** La puerta del clic irreversible pasa de 1 de 4 rutas a **4 de 4** (`click`, `click_at` por
visión, `press`, `type --submit`) y deja de juzgar el RÓTULO — que fallaba 21 de 30 etiquetas reales,
«Reservar» incluida, el caso insignia del producto — para juzgar el **contexto**: un submit dentro de un
formulario que pide tarjeta/IBAN/contraseña/identidad, o un destino en ruta de compra/pedido/reserva. Y falla
**CERRADA**: antes, si no se podía leer el elemento, se pulsaba igual. 30/30 en las etiquetas medidas.

Los tres modos en sombra (árbitro del canvas V2-653 F0, encargos, y el nuevo `gate_shadow`) **ya tienen
lector**: `/api/observability/shadow`. Contra datos reales dio el número que la promoción de V2-653 llevaba
esperando desde que se escribió el gate: **28 de 200 veredictos habrían parado algo** (data-drag 18,
show-drag 10).

El guarda del contrato V2-705 deja de contradecirse (`is_destructive` en el embudo, no `classify`), la puerta
genérica `rows.*` deja de estar en el deny del worker que la estrenó, `nucleo/danger.py` recupera sus 93
comentarios y el latido del conector deja de comerse el registro de eventos.

**Regresión propia, registrada:** componer `_MONEY_ACT_RE` entero aparcó «renueva el gráfico del widget». La
cazó `test_search_report_flow.py`, que **no corrí antes de commitear T0.4**. Correr la suite del módulo no es
correr la de sus llamadores.

⛔ **Incidente de cierre:** se lanzó `tests/infrastructure/unit/` para «verificar que todo está alineado» y se
quedó **media hora sin producir nada**, con la sesión bloqueada y el trabajo a punto de perderse. La
prohibición de `tests/README.md` **ya nombraba esa carpeta exacta**. Segunda medición escrita allí, con la
receta portable (macOS no tiene `timeout` ni hay `pytest-timeout` en el venv): `perl -e 'alarm shift; exec
@ARGV' 180 … -q`, **un fichero por invocación**.

**Verificación**: 12 ficheros a solas, **~700 verdes, cero rojos**, ninguno por encima de 4 s. Único rojo del
árbol: `test_claude_md_ratchet` — `CLAUDE.md` está a 402 KB en el working tree con 3.541 líneas **sin
commitear de otra sesión** (HEAD son 57 KB, verde). No es de esta tanda y no se ha tocado ese fichero.

**No entregado** (y dicho como lo que es): T2.1 el test del turno de voz real (~2 jornadas), T2.3 el almacén
de políticas V2-707 F4, T2.4 la confirmación de `rows.*` fuera de banda, T2.6 los contadores del supervisor,
T2.7 la deuda de tests rojos previa de V2-707, y **todo T3** — que estaba condicionado desde el principio a
leer la sombra, y la sombra acaba de empezar a medir hoy.

## 18:20 · V2-709 F6 · el visor sigue a los agentes, a varios a la vez, y deja de lanzar tests

Segunda mitad del encargo del visor. Medido: el servidor estaba atado a UN run y cada corrida MATABA
al visor anterior — con dos agentes sobre el mismo checkout, el segundo borraba al primero de la
pantalla y nada lo decía. Ahora el servidor sirve la raíz de runs (`/api/runs`, `/events` multiplexado
con `run_id` por línea, `?run=` para uno), el CLI reutiliza un visor sano, y la pantalla sigue sola al
run que empezó PRIMERO sin que un segundo le robe el foco. Columna izquierda parpadeando en azul en
las suites bajo prueba + caja de agentes abajo con una línea por agente. Fuera el ▶ y el endpoint
`/api/run`: los tests los conducen los agentes.

Verificado con dos agentes simultáneos reales escribiendo eventos y Playwright mirando, más una corrida
real de `python -m tests run` con el visor abierto (mismo PID antes y después). Cinco desarmes en rojo.

Commit: `e2c48f70`.

## 21:05 · V2-712 — una sola regla de consentimiento (T0+T1+T2, 3 commits)

Corrección del operador en vivo sobre lo que acababa de entregar V2-711: «estoy un poco hasta los huevos de
tener que confirmar las cosas». Y la corrección de diseño, que manda: «no forzamos las preguntas de forma
hardcodeada; tiene que ser el propio sistema el que tenga UNA REGLA para todo el sistema».

Tenía razón y el defecto era mío: V2-711 puso el gate del clic a pedir consentimiento cuando el formulario
llevaba datos de IDENTIDAD, que es la forma NORMAL de la orden que él acababa de dar.

`nucleo/consent.py` hace cuatro preguntas en su orden — ¿falta un dato? (pide el DATO) → ¿hay duda? (di
cuáles) → ¿el radio excede lo que nombró? (carril de V2-707, intacto) → ¿la clase es sensible? El nivel es
DATO declarado en capas como `style_policy` (V2-633, que era la plantilla y no había que inventarla): 15
widgets declaran `security: standard`, `confirm:true` se LEE como `sensitive` en vez de borrarse, y dos
propiedades nuevas separan lo que «sin selector» confundía: `singleton` y `fans_out`.

Su ejemplo del autorrespondedor entero: `messaging.autorespond: never` en el Génesis es un SUELO leído en la
boca, y encenderlo ES él cambiando la regla, así que `set_config` escribe `allow`. Las dos mitades juntas.

⚠️ 22 tests de ayer afirmaban la fricción que él ha rechazado. Reescritos para afirmar el contrato nuevo, cada
uno diciendo en su docstring qué afirmaba antes y por qué se invirtió — no silenciados.

Quince desarmes, todos rojos. 50 casos nuevos + 70 reescritos. ABIERTO T3.

## 23:40 · V2-713 — segunda auditoría externa, ejecutada (T0+T1+T2, 3 commits)

`94550c11` T0 · `f3f674d0` T1 · `16746e32` T2. Los cinco hallazgos (R1–R5) se confirmaron re-midiéndolos
sobre el árbol de hoy; ninguno lo cubría ya V2-711/V2-712.

**R2** — el resumen decía `errors=0` sobre un worker muerto: **30 de 30** flujos con tarea muerta, sobre la
base real. `ok` no es columna del bus y el payload es PLANO (`$.ok`, no `$.extra.ok` — la primera versión de
la consulta buscó donde no era). Contador APARTE `failed_tasks` en las tres proyecciones, con DISTINCT.

**R4** — un `broken` SIN relevo ponía el proveedor en cooldown y no emitía ningún chip, porque
`classify_failure` no reconoce lo que sí reconoce `is_broken_request`. La causa se clasifica UNA vez y viaja
como dato; el chip rotula por kind en vez de decir siempre «sin cuota».

**R1** — el guarda del show vetaba activaciones correctas. La salida NO fue ampliar la tabla de verbos sino
que el widget DECLARE `activation: true`; sin declarar = bloqueado = el comportamiento de hoy. Dos
heurísticas intermedias murieron contra `clear_all` y `navegador:open`, cazadas por un test que YA existía.
Cada veto va a sombra para poder retirar el guarda con un número.

**R5** — «1-2 frases» y «UNA ACCIÓN por turno» al Génesis. El turno CRECIÓ 88 bytes y se dice: lo que compra
es que él las cambia hablando. Y el trinquete de prosa descubrió 402 bytes de `prompt_line()` invisibles
desde V2-633.

**R3** — ya estaba medio hecho (el trinquete de espejos cuenta las dos marcas desde V2-711 T0.6). Se extrajo
el cierre corto a `close_guards`, y el test encontró dos sitios más con el mismo `<= 5` suelto.

Veinte desarmes rojos. `router_guards` 742→667 pagado extrayendo `show_guard.py`. ABIERTO: T2.1, el test del
turno de voz real.
